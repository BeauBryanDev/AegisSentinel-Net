/*
 * Placeholder: requires authenticated user context.
 * Will display operator profile, role, and activity log.
 */
export default function UserProfile() {
  return (
    <section className="hud-panel p-4">
      <p className="hud-label">Operator Profile</p>
      <p className="mt-3 text-sm text-silver-300">
        Module pending authentication system deployment.
      </p>
    </section>
  );
}